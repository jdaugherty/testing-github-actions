name: "Testing Post Release"
on:
  release:
    types: [published]
jobs:
  testing_job:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 'release - no nested folder'
        uses: jdaugherty/grails-github-actions/post-release@asf